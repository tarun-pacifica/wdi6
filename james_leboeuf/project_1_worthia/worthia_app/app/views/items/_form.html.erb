<%= form_for @item do |f| %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name, :maxlength => 30, :size => 20, :required => true %>
  </p>

  <p>
    <%= f.label :content %>
    <%= f.text_field :content, :maxlength => 140, :size => 20, :required => true %>
  </p>

  <p>
    <%= f.label :image %>
    <%= f.url_field :image %>
  </p>

  <%= fields_for :price do |p| %>
    <p>
      <%= p.label :price %>
      <%= p.number_field :price, in:(1..10000), :required => true %>
    </p>

  <% end %>

<label></label>
<%= f.submit 'Create' %>

<% end %>