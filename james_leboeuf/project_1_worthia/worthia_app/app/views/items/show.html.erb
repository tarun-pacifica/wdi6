<h2><%= @items[:name] %></h2>
<%= image_tag(@items.image) %>
<h2><%= number_to_currency(@price) %></h2>
<%= link_to('Edit', edit_item_path(@items)) %>
<%= link_to('Delete', @items, :method => :delete, :data => {:confirm => 'Are you sure?' }) %>